#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */

int  main(void)
{
	char j;

	j = 'a';
	while
		(j <= 'z') {
			if ((j != 'q' && j != 'e') && j <= 'z')
				putchar(j);
			j++;

	}
	putchar('\n');
	return (0);
}
